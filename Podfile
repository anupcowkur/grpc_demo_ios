source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'

install! 'cocoapods', :deterministic_uuids => false

target 'grpc_demo_ios' do
  # Depend on the generated grpc_demo_ios_client library.
  pod 'grpc_demo_ios_client', :path => '.'
end
